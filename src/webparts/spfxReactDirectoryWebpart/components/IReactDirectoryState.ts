export interface IReactDirectoryState {
  users: unknown;
  isLoading: boolean;
  errorMessage: string;
  hasError: boolean;
  indexSelectedKey: string;
  searchString: string;
  searchText: string;
}
